export enum TransactionStatusEnum {
  success = 'success',
  pending = 'pending',
  fail = 'fail',
  invalid = 'invalid',
  executed = 'executed',
  notExecuted = 'not executed',
  rewardReverted = 'reward-reverted',
}
