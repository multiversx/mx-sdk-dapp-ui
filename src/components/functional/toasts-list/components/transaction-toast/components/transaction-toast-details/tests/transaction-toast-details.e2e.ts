// transaction-toast-details is now a plain functional component and cannot be
// tested as a web component with newE2EPage.
// See transaction-toast-details.spec.tsx for unit tests.

describe('transaction-toast-details', () => {
  it('is tested in transaction-toast-details.spec.tsx', () => {
    expect(true).toBe(true);
  });
});
